<manifest xmlns:android="http://schemas.android.com/apk/res/android" 
    xmlns:tools="http://schemas.android.com/tools"
    package="com.khtwah.kanzalsahra">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <queries>
        <provider android:authorities="com.facebook.katana.provider.PlatformProvider" />
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Other intents -->
    </queries>

    <application
        android:name="${applicationName}"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:extractNativeLibs="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic,android:allowBackup">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Add your other intent filters here -->
        </activity>

        <!-- Change the authorities of content providers to avoid conflicts -->
        <provider
            android:name="com.facebook.FacebookInitProvider"
            android:authorities="com.khtwah.kanzalsahra.FacebookInitProvider"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.khtwah.kanzalsahra.androidx-startup"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="com.khtwah.kanzalsahra.firebaseinitprovider"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="io.flutter.plugins.imagepicker.ImagePickerFileProvider"
            android:authorities="com.khtwah.kanzalsahra.flutter.image_provider"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="com.example.flutter_share.FlutterShareContentProvider"
            android:authorities="com.khtwah.kanzalsahra.flutter.share_provider"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="io.flutter.plugins.firebasemessaging.FlutterFirebaseMessagingInitProvider"
            android:authorities="com.khtwah.kanzalsahra.flutterfirebasemessaginginitprovider"
            android:exported="false"
            tools:replace="android:authorities" />

        <provider
            android:name="com.google.android.gms.ads.MobileAdsInitProvider"
            android:authorities="com.khtwah.kanzalsahra.mobileadsinitprovider"
            android:value="ca-app-pub-3940256099942544~3347511713"/>
            android:exported="false"
            tools:replace="android:authorities" />

        <!-- Other configurations like meta-data, services, etc. -->
        <meta-data android:name="flutterEmbedding" android:value="2" />

    </application>
</manifest>
