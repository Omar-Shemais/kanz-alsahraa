# تسليم تطبيق كنز الصحراء — 19 سبتمبر 2026

## حالة التسليم

المشروع أصبح في مرحلة **مرشح للاختبار قبل النشر**، وليس إصدار متجر نهائياً بعد. تم دمج ومراجعة أعمال المالك وAntigravity ومراجعة Codex، وبناء نسخة Android تجريبية بنجاح. لا يجوز وصف النسخة بأنها خالية تماماً من الأخطاء قبل إتمام اختبارات الأجهزة والمتاجر والأمان المذكورة أدناه.

## ما تم تنفيذه والتحقق منه

- ربط الصفحة الرئيسية بملف MStore الحي مع نسخة محلية احتياطية وكاش آمن.
- لوحة WordPress عربية لإدارة بانرات وأقسام الصفحة الرئيسية ووجهات الضغط وترتيب التصنيفات والتحديث الإجباري والإشعارات.
- حفظ النسخ السابقة من إعدادات الصفحة ومنع الكتابة المتزامنة أو نشر JSON غير صالح.
- ربط Firebase للإشعارات، Analytics، Crashlytics وPerformance Monitoring في Android وiOS.
- استبدال Firebase Dynamic Links المتوقفة بروابط Android App Links وiOS Universal Links على `kanzalsahra.com`.
- نشر ملفي الارتباط على الموقع والتحقق من استجابتهما بنجاح، والتحقق من أن بصمة Android المنشورة مسجلة في Firebase.
- تحسين أمان طلبات الشبكة، شاشة الطلبات، التخزين المحلي، تسجيل الدخول والخروج، والسلة والدفع.
- إزالة بوابات الدفع والحزم غير المستخدمة التي شملتها خطة التنظيف.
- جعل التطبيق عربياً فقط، بما في ذلك تصحيح الأجهزة التي تحتفظ بلغة قديمة من إصدار سابق.
- تصحيح اتجاه الأسهم والنصوص في الواجهات العربية.
- إيقاف السحب الذي كان يعيد بناء الصفحة الرئيسية ويعيد موضع التمرير، وتقليل التحميل المبكر لأقسام المنتجات.
- إيقاف تهيئة Facebook غير المستخدمة ومنع طباعة مفاتيح WooCommerce أو cookie أو رموز FCM في السجل.
- تحسين تسجيل رمز الإشعارات في iOS بعد جاهزية APNs، ومزامنة تغير الرمز مع حساب WordPress المسجل.

## تحقق البيئة الحية

- ملف MStore الحي وواجهة REST يعيدان JSON صالحاً ومتطابقاً، وبداخله 11 قسماً و4 تبويبات.
- لوحة WordPress تقرأ ملف MStore تلقائياً ولا تحتاج رفع الملف في كل زيارة.
- حساب خدمة Firebase ظاهر في اللوحة على أنه مربوط بالمشروع `kanz-alsahra`.
- روابط Android وiOS على الموقع تعيد حالة HTTP 200 وJSON صالحاً.
- Hostinger يحتفظ بنسخة يومية حديثة، وآخر نسخة ظاهرة كانت بتاريخ 18 سبتمبر 2026 الساعة 20:05.
- WordPress وPHP يعملان، لكن لوحة Hostinger تعرض تحديثات متاحة لـ17 إضافة وحالة الموقع «تحتاج إجراء»؛ لا تنفذ تحديثات جماعية قبل نسخة اختبار.
- مسارات REST المطلوبة للحساب موجودة على الموقع الحي: إنشاء جلسة الدخول، تحديث الملف الشخصي، إعادة كلمة المرور، حذف الحساب، ومزامنة السلة. تم التحقق من وجود المسارات فقط؛ لم يُرسل OTP ولم يُحذف حساب ولم تُنفذ عملية شراء.
- مراجعة كتالوج WooCommerce الحي وجدت 163 منتجاً منشوراً، وجميعها تحمل SKU. توجد 4 مجموعات SKU مكررة تشمل 8 منتجات، كما أن المنتجات الـ163 لا تستخدم خصائص WooCommerce المنظمة حالياً؛ يلزم تنظيفها قبل اعتبار مزامنة الكتالوج وقواعد السبائك مكتملة.

## نتائج الاختبار

- آخر استكمال: 211/211 اختبار ناجح، وفحص أسرار المصدر ناجح، وبناء debug ناجح مجدداً. البناء الأخير دون قيم Woo الخارجية هو تحقق تجميع فقط، وليس نسخة تسليم وظيفية.
- محرر ترتيب التصنيفات يرفض الآن أرقام المواضع الكسرية بدلاً من تقريبها بصمت؛ الإصلاح محلي واختباره ناجح، ولم يرفع إلى WordPress.

- 210 اختبار Flutter آلي نجحت في التشغيل الكامل.
- اختبارات الإشعارات والوجهات ومراجعة Antigravity نجحت بعد آخر تعديلات الإشعارات.
- تحليل الملفات المعدلة انتهى دون أخطاء.
- التحليل الكامل للمستودع لا يزال يسجل 525 ملاحظة قديمة: 58 تحذيراً و467 ملاحظة، دون أخطاء تحليل من مستوى `ERROR`. أغلبها إهمالات API وأسلوب/كود قالب غير مستخدم؛ لذلك لا يصح وصف المستودع بأنه نظيف بالكامل رغم نجاح البناء والاختبارات.
- بناء Android debug APK نجح: `build/app/outputs/flutter-apk/app-debug.apk`.
- سجل Android أكد بدء Firebase وCrashlytics، وكشف قبل الإصلاح عن سقوط إطارات وطلبات متزامنة بطيئة؛ عولجت أسباب مباشرة، ويجب إعادة القياس على نسخة release وجهاز حقيقي.

## نقاط تمنع تسليم إصدار متجر نهائي

1. أزيلت بيانات WooCommerce وكلمات مرور التوقيع من المصدر، ونقلت حاويات التوقيع الثلاث إلى مجلد خاص خارج المشروع مع التحقق من بصماتها؛ فحص release_security ينجح الآن. يلزم إعداد متغيرات البناء المشفرة وتدوير المفتاح المكشوف سابقاً. متغيرات dart-define ليست حماية للمفتاح داخل التطبيق النهائي؛ الحل الأقوى هو وسيط آمن على الخادم بصلاحيات محدودة.
2. اعتمد المالك نشر ترتيب التصنيفات مع تعطيل التحديث الإجباري. محاولة المتصفح لم تتم بنجاح، والتحقق العام لم يجد KanzControl داخل JSON الحي؛ لا يعد هذا البند منشوراً حتى يظهر فعلياً في الرابط العام.
3. التحديث الإجباري غير مفعل، وهذا صحيح حتى تتوفر نسخة جديدة فعلياً في Google Play وApp Store.
4. تسجيل الدخول فُحص برمجياً، لكن لم تنفذ عملية OTP حقيقية أو تسجيل دخول بحساب عميل أثناء المراجعة. يلزم حساب اختبار مخصص.
5. iOS لا يمكن بناؤه أو توقيعه محلياً على Windows، لكن Codemagic يوفر بيئة macOS للبناء والتوقيع والنشر إلى TestFlight. يبقى iPhone حقيقي ضرورياً للتحقق من APNs وUniversal Links وتسجيل الدخول والدفع.
6. Crashlytics وPerformance مدمجان في الكود، لكن يجب التأكد من ظهور جلسة release وبيانات الأداء في Firebase بعد تشغيل النسخة الجديدة على أجهزة حقيقية.
7. لا ترسل إشعاراً عاماً أثناء الاختبار. استخدم «اختبار حساب واحد فقط» بعد تثبيت النسخة الجديدة وتسجيل دخول حساب الاختبار حتى يُحفظ رمز الجهاز.
8. تنظيف 4 مجموعات SKU المكررة وإضافة خصائص منظمة للوزن والعيار ونوع المعدن حيث تنطبق، ثم إعادة اختبار البحث والتصنيف والسلة.
9. مسارات حذف الحساب ومزامنة السلة موجودة على الخادم، لكن القبول الوظيفي يحتاج حساب اختبار وبيانات غير حقيقية؛ وجود المسار وحده لا يثبت صحة السلوك أو الصلاحيات.
10. تنظيف تحذيرات التحليل ذات الصلة بمسار الإنتاج أولاً، ثم فصل أمثلة الحزم والأجزاء غير المستخدمة عن نطاق تحليل التطبيق لتكوين بوابة CI قابلة للتنفيذ.

## خطوات التسليم الآمن

1. إنشاء نسخة staging أو نسخة مستنسخة من الموقع للاختبارات القادمة؛ الموقع الحالي إنتاجي.
2. تدوير مفتاح WooCommerce وحصر صلاحياته، ثم إزالة السر القديم من تاريخ المستودع والسجلات.
3. إخراج ملفات التوقيع وكلمات المرور من حزمة التسليم، وحفظ نسخة مشفرة لدى المالك.
4. نشر ترتيب التصنيفات من لوحة WordPress بعد اعتماد العميل للترتيب، مع إبقاء التحديث الإجباري معطلاً.
5. تثبيت نسخة Android release موقعة على جهاز حقيقي واختبار: فتح التطبيق، التمرير، ضغط المنتجات، البحث، الدخول، السلة، الشحن والدفع التجريبي.
6. بناء iOS عبر Codemagic ونشره إلى TestFlight، ثم اختبار إشعار لحساب اختبار واحد على iPhone حقيقي في المقدمة والخلفية وبعد الإغلاق.
7. مراجعة Crashlytics وPerformance بعد جلسات الاختبار، وإغلاق أي أعطال جديدة قبل رفع المتاجر.
8. رفع الإصدار إلى قنوات الاختبار الداخلية أولاً، ثم الإنتاج بعد توقيع قبول الاختبارات.
9. إجراء تدقيق نهائي للكتالوج بعد إزالة SKU المكرر وتوحيد خصائص المنتجات، والاحتفاظ بتقرير قبل/بعد.

## تفاصيل تعارضات SKU التي تحتاج مراجعة المتجر

- `55743cc0393b`: المنتج 20258 والمنتج 22662.
- `5c0492567492`: المنتج 22696 والمنتج 20309.
- `8757150decbd`: المنتج 22684 والمنتج 20291.
- `eaae339c4d89`: المنتج 20264 والمنتج 22666.

لا يُحذف أي منتج تلقائياً؛ يراجع مسؤول المتجر ما إذا كانت العناصر مكررة فعلاً أو تحتاج رموزاً مستقلة.

## تشغيل لوحة WordPress للعميل

- البانرات: اختيار صورة، ثم اختيار وجهة الضغط (تصنيف، منتج، صفحة، أو رابط HTTPS).
- أقسام المنتجات: اختيار التصنيف وعدد المنتجات وطريقة الترتيب.
- التصنيفات: استخدام الأسهم أو رقم الموضع ثم نشر الإعدادات.
- الإشعارات: البدء دائماً باختبار حساب واحد؛ لا تستخدم البث العام قبل قبول النسخة.
- التحديث الإجباري: لا يُفعّل إلا بعد توفر رقم البناء المطلوب في المتجرين.

## إعداد البناء والاستكمال

- ملفات التوقيع محفوظة خارج المستودع في `C:\Users\omar2\Documents\Kanz_Private_Signing_2026-09-19`: `Kanzalsahrakey.jks` و`keystoreMatjarPanda.jks` و`mstore.jsk`. لم تحذف نهائياً؛ يجب حفظ نسخة مشفرة لدى المالك، وعدم رفعها في حزمة العميل أو Git.
- إعداد Android يتطلب `KANZ_KEYSTORE_PATH` و`KANZ_STORE_PASSWORD` و`KANZ_KEY_PASSWORD` و`KANZ_KEY_ALIAS`. يجب استخدام مفتاح التطبيق المنشور نفسه؛ لا تنشئ مفتاحاً بديلاً عشوائياً.
- إعداد WooCommerce يتطلب `KANZ_WOO_CONSUMER_KEY` و`KANZ_WOO_CONSUMER_SECRET` في متغيرات Codemagic المشفرة، وتمريرهما إلى Flutter عبر `--dart-define=KANZ_WOO_CONSUMER_KEY=$KANZ_WOO_CONSUMER_KEY` و`--dart-define=KANZ_WOO_CONSUMER_SECRET=$KANZ_WOO_CONSUMER_SECRET`. لا تطبع القيم في سجلات البناء. البناء دونها لا يثبت عمل الكتالوج.
- أضيف استحقاق Sign in with Apple إلى `ios/Runner/Runner.entitlements` الفعلي. يجب مطابقة capability في Apple App ID وملف provisioning المستخدم في Codemagic.
- لا يوجد `codemagic.yaml` في المشروع حالياً؛ إعدادات واجهة Codemagic والتوقيع تحتاج تحققاً قبل إصدار المتاجر.
- امتداد OneSignal القديم ما زال موجوداً في iOS رغم تعطيل حزمة Flutter؛ لا يعد ضرورياً لإشعارات FCM النصية، لكنه يبقى حتى التحقق من بناء Codemagic قبل تنظيف أهداف Xcode.
