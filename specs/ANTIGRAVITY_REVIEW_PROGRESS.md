# مراجعة Antigravity ومتطلبات إدارة تطبيق كنز

## مراجعة التسليم النهائية — 19 سبتمبر 2026

- المجموعة الكاملة الحالية: 210 اختبارات Flutter ناجحة. تحليل ملفات التطبيق والإشعارات المعدلة انتهى دون أخطاء، وبناء Android debug الجديد نجح. التحليل الكامل للمستودع سجل 525 نتيجة (58 warning و467 info، بلا ERROR)، لذلك المستودع القديم ليس نظيف التحليل بالكامل.
- عولج سبب مباشر لقفز الصفحة الرئيسية: أُلغي السحب الذي كان يعيد تحميل إعدادات التطبيق كاملة ويعيد موضع التمرير، وخُفض التحميل المبكر للأقسام. أوقفت كذلك تهيئة Facebook غير المستخدمة التي كانت تولد طلبات فاشلة، ومنعت تسجيل مفاتيح WooCommerce وcookies ورموز FCM.
- عُززت دورة رمز FCM: انتظار APNs في iOS، معالجة token refresh، وإعادة ربط الرمز بالمستخدم المسجل مع إلغاء الربط المحلي عند الخروج. يمكن تنفيذ بناء وتوقيع iOS عبر Codemagic؛ يلزم iPhone حقيقي لإثبات APNs عملياً.
- تسجيل Apple مفعّل في إعدادات التطبيق، بينما ملف Runner.entitlements المستخدم في كل إعدادات Xcode كان يفتقد `com.apple.developer.applesignin`؛ أضيفت الصلاحية إلى الملف الفعلي. يجب تفعيل Sign in with Apple على App ID الرئيسي وإعادة توليد profile صالح بعد تغيير capability.
- التطبيق يجبر العربية عندما تكون اللغات المتعددة معطلة، حتى لو بقيت لغة قديمة في تخزين إصدار سابق.
- البيئة الحية تعيد ملفات App Links وUniversal Links وMStore/REST بحالة 200 وJSON صالح. إعداد ترتيب التصنيفات ما زال مسودة في WordPress ولم يُنشر إلى JSON الحي.
- فحص الكتالوج الحي: 163 منتجاً منشوراً، لا SKU مفقود، لكن 4 مجموعات SKU مكررة تشمل 8 منتجات، وجميع المنتجات بلا خصائص WooCommerce منظمة. لا يجوز إغلاق مهام تنظيف الكتالوج/خصائص الذهب قبل المعالجة.
- مسارات REST للحساب والحذف والسلة موجودة حياً؛ لم يُختبر OTP أو الحذف أو الشراء بحساب حقيقي، لذلك يبقى قبولها الوظيفي مفتوحاً.
- بوابة الأمان ما زالت تمنع إصدار release بسبب مفتاح WooCommerce مضمّن وأسرار/ملفات توقيع داخل المشروع. لا تحذف مفتاح توقيع التطبيق الحالي؛ انقله إلى تسليم مشفر ودوّر مفتاح WooCommerce.


## تنفيذ مراقبة التطبيق واستبدال Dynamic Links — 18 سبتمبر

- أضيفت حزم Crashlytics وPerformance المتوافقة مع Firebase Core الحالي، مع إضافات Gradle وربط الأخطاء ومدة طلب الكتالوج. أحداث Analytics الجديدة تحمل kanz_source=mobile_app وkanz_platform لتمييز التطبيق عن الموقع. لا توجد أحداث شراء وهمية.
- نسخة debug الاختبارية تدعم KANZ_TELEMETRY_SMOKE لإرسال حدث Analytics وخطأ غير قاتل وtrace باسم kanz_monitoring_check. لا يعمل هذا الاختبار في release ولا يتعمد إسقاط التطبيق.
- استبدل Firebase Dynamic Links بـapp_links وروابط HTTPS على نطاق المتجر فقط، مع ضبط Android وiOS وتعطيل معالج Flutter المنافس. روابط page.link القديمة لا يمكن إحياؤها بعد إيقاف الخدمة.
- أضيف script رفع رموز Crashlytics في Xcode للإصدارات غير debug. إعداد iOS المحلي صحيح للمشروع، لكن بناء Mac واختبار iPhone ووصول القياسات غير منفذة حتى الآن.
- كلا رابطَي /.well-known/assetlinks.json و/.well-known/apple-app-site-association يرجعان 404 على الموقع الحي. جهز snippet مستقل غير منشور لتقديمهما عبر WordPress؛ يحتاج Android شهادة SHA-256 من Play App Signing والتحقق من وصول الطلب إلى WordPress.
- 11 اختباراً مركزاً نجحت: سياسة الروابط، عدم تعطيل العملية عند غياب SDK، ووجهات الإشعارات. PHP lint للـsnippet نجح. بناء APK والتحليل ما زالا قيد التحقق؛ لا تعد القياسات مثبتة في Firebase حتى اختبار النسخة الجديدة.
- هذه تغييرات محلية فقط؛ لم تتغير إعدادات WordPress أو مفاتيح أو الفوترة، ولم ترسل إشعارات للعملاء. لا يغلق هذا العمل تلقائياً بقية قائمة الـ38 مشكلة ولا يثبت تحسن السرعة قبل المقارنة الفعلية.
- المجموعة الكاملة بعد الإضافة: 203 اختبارات Flutter ناجحة، exit 0. ستة فحوص PHP لملفات association نجحت. التحليل الأول لا errors أو warnings، لكنه أعاد 5 ملاحظات info حول ترتيب imports والأقواس؛ صُححت محلياً ويلزم قراءة نتيجة الإعادة. حُدث Google Services إلى 4.4.2 لتلبية متطلب Crashlytics v3؛ بناء ما قبل هذا التعديل ليس إثباتاً للبناء النهائي.
- بناء Android الأول نجح بعد 1266.9 ثانية. الجهاز وصل إلى 74MB ذاكرة متاحة؛ أُوقفت فقط شجرة إعادة التحليل الخاصة بهذه المهمة لتخفيف الضغط (لذلك نتيجتها غير مكتملة)، وبقيت عمليات المستخدم والبناء. أُعيد البناء بعد تحديث Google Services، وهو قيد التحقق. واجهة Chrome توقفت عند Debugger unattached؛ لم تثبت مشاهدة القياسات في Firebase بعد.
- كشف تتبع Analytics خللاً إضافياً: Woo WebView يمسح السلة قبل triggerPurchased، والـStreamController غير المتزامن يمكنه قراءة سلة أُفرغت أيضاً في الدفع الأصلي. صُحح ترتيب التسجيل، وأصبح توصيل الأحداث متزامناً لأخذ القيم قبل clearCart. تسجيل Firebase يستخدم id أو number الصحيح ولا يخترع مرجعاً عند غيابه. أضيفت ثلاثة اختبارات لمرجع العملية؛ تحتاج النتيجة الحالية وبناء لاحق يشمل هذا التعديل. لا يُعد هذا إثباتاً لتأكيد الدفع أو منع كل ازدواج مبيعات أو تطابق أسعار الطلب النهائي.
- البناء الثاني فشل بعد 627.5 ثانية: Gradle daemon disappeared، لا خطأ ترجمة ظاهر. ضغط الذاكرة محتمل وليس سبباً مؤكداً. أُعيد البناء مع GRADLE_OPTS مؤقت لهذا الأمر فقط: heap=2048MB، عامل واحد، parallel=false، وandroid-x64 للمحاكي. لا تعديل دائم لإعدادات بناء المستخدم ولا إيقاف لبرامجه؛ آخر APK سابق لا يثبت احتواء كل التعديلات.
- بعد إصلاح مسار Analytics: المجموعة الكاملة 206 اختبارات ناجحة، exit 0 (concurrency=1). metadata الخاصة بFlutter تسجل SDKs الأربعة على Android وiOS؛ هذا لا يعوض بناء/اختبار iPhone. تأكد أن daemon الجديد يستخدم heap=2048MB فعلاً. البناء الحالي ما زال قيد التحقق.
- التحليل المعاد لعشرة ملفات مراقبة/روابط/Analytics: No issues found، exit 0، بعد إصلاح الملاحظات. لا يعد هذا تحليلاً كاملاً لكل ملفات القالب. مجموعة 206 اختبارات نجحت على أحدث تعديل وظيفي؛ تعديلات لاحقة كانت أقواساً وتوثيقاً فقط.
- البناء المحدود android-x64 نجح (527.5 ثانية، exit 0) بإعداد Google Services المتوافق. محاولة تحديث emulator-5554 فشلت لأن الجهاز غير متصل: device not found. لم يُحذف التطبيق أو تُمسح بياناته، ولم تثبت النسخة الجديدة على محاكي من هذه المحاولة. يلزم جهاز متصل لاستكمال smoke SDK ثم تأكيد لوحة Firebase.

## فحص Firebase عبر إضافة Chrome — 18 سبتمبر

- الوصول إلى kanz-alsahra نجح بالحساب المفتوح في Chrome. فحصنا FCM/APNs والتقارير وAnalytics وربط streams وPhone providers وسياسة مناطق SMS وFirestore/Remote Config/Crashlytics/Performance/Storage/App Check للقراءة فقط.
- التفاصيل وخطة الإغلاق في specs/FIREBASE_LIVE_AUDIT_2026-09-18.md. لا رسائل أو SMS أو تغيير خطة/مفاتيح/صلاحيات، ولا فتح بيانات العملاء.
- النتائج الرئيسية: APNs موجود ويطابق فريق Xcode؛ Analytics مرتبط لكن البيانات الظاهرة أساساً للموقع ولا تثبت قياس التطبيق؛ Crashlytics وPerformance SDK غير موجودين؛ Dynamic Links ما زال مفعلاً بالكود رغم إيقاف الخدمة؛ APNs readiness وtoken refresh تحتاج معالجة واختبار جهاز. هذه مراجعة وليست تنفيذ إصلاحات جديدة.


## استلام إعداد Firebase الصحيح لـiOS — 18 سبتمبر

- المسار المرفق Downloads/GoogleService-Info.plist غير موجود؛ عثرنا على الملف داخل Downloads/kanz backup/GoogleService-Info.plist. تحققنا من project_id=kanz-alsahra وsender=132198990660 وbundle_id=com.khtwah.kanzalsahra وApple app ID بالمشروع نفسه.
- استبدلنا إعدادات configs/GoogleService-Info.plist وios/GoogleService-Info.plist معاً، لأن pre-action الخاص بـXcode ينسخ الأولى إلى الثانية. بقي ملف المستخدم الأصلي دون تعديل.
- هذا تصحيح محلي لإعداد البناء، وليس تغييراً في إصدار App Store الموجود. يبقى APNs والتوقيع وتسلسل جاهزية APNs token واختبار iPhone/فتح وجهة الإشعار غير مؤكد. لم ترسل أي رسالة ولم يحدث تعديل WordPress في هذه الجلسة.


## محاولة نشر WordPress وفحص iOS — 18 سبتمبر، جلسة الموقع الحي

- فحصنا Snippet رقم 49: إدارة تطبيق كنز الصحراء، فعال، Run everywhere. لم نحفظ أو نستبدل كوده في هذه الجلسة. اختيار ملف الاستيراد تعطل مرتين؛ الصفحة ما زالت بالنسخة السابقة. لم تنشأ نسخة مكررة ولم يتغير JSON الحي ولم ترسل إشعارات أو تفعل سياسة تحديث إجبارية.
- زر Export لم ينتج ملفاً محلياً مؤكداً؛ لا تعتبر نسخة ما قبل التعديل محفوظة من هذه المحاولة. أُلغي طلب إضافة VirusTotal مشاركة الملف، ولم توافق الجلسة على رفعه إليها.
- Firebase Console للحساب المفتوح يعرض أن مشروع kanz-alsahra غير موجود أو لا توجد صلاحية لسرد تطبيقاته. يلزم فتح الحساب صاحب المشروع قبل مراجعة APNs أو تنزيل ملف iOS الجديد؛ لم تنشأ أو تحفظ مفاتيح جديدة.
- مشكلة مؤكدة: ios/GoogleService-Info.plist وconfigs/GoogleService-Info.plist يشيران إلى kanzalsahra-7e458 (المشروع القديم). يجب تنزيل GoogleService-Info.plist من تطبيق Apple داخل kanz-alsahra واستبدال الملفين بعد التحقق من bundle ID؛ لا يمكن اشتقاق معرف تطبيق Apple من ملف Android.
- مشكلة مؤكدة ثانية: FirebaseAppDelegateProxyEnabled كان false؛ صُحح محلياً إلى true في ios/Runner/Info.plist طبقاً لمتطلبات Flutter FCM. لم يبن أو يثبت إصدار iOS، ولا يعد هذا إثبات وصول الإشعار.
- Push capability وremote-notification وaps-environment موجودة في المشروع؛ قيمة البيئة production ليست إثبات تطابق التوقيع أو APNs. getToken/subscribeToTopic عند بدء التشغيل لا ينتظران APNs وقد يفشلان دون إعادة محاولة؛ تبقى مراجعة هذا التسلسل مع token refresh مطلوبة قبل إغلاق بند iOS.
- أُعيد php lint و13 فحص MStore/تشفير/جمهور منعزل بنجاح. لا وصول لخادم الاستضافة مطلوب لإدارة الواجهة، لكن إدخال مفتاح الخدمة وAPNs يتم بواسطة المستخدم مباشرة، لا داخل المحادثة أو الوسائط.


## أحدث طلب: مصدر MStore واحد واختبار إشعار بحساب — 18 سبتمبر

- راجعنا أحدث سجل Antigravity: أضاف واجهة ترتيب التصنيفات والوجهات وإعداد مفتاح من لوحة المسؤول. ادعاء اختفاء 4000ms أو تشغيل الإنتاج 100% غير معتمد دون قياس ومراجعة مستقلة. احتفظنا بتعديلاته وواجهة البحث والسحب.
- ملف النشر المعتمد wordpress/snippets/kanz-app-control-snippet.php أصبح يقرأ config_ar.json الموجود في MStore تلقائياً عند فتح اللوحة، ويكتب الملف نفسه باستبدال ذري، مع بصمة تعارض وحفظ نسخة سابقة؛ REST يقرأ نفس المصدر. لا حاجة لاستيراد متكرر أو تغيير appConfig عندما يستخدم الملف نفسه. يعتمد على صلاحيات كتابة PHP، وFlutterUtils يدعم مسار MStore القديم والجديد. لا تكتب من واجهتين بالتزامن؛ CDN خارجي يحتاج تحقق.
- أصلحنا تخزين Antigravity لمفتاح Firebase بالنص الصريح: حفظ AES-256-GCM مشتق من WordPress salts، عبر HTTPS للمسؤول فقط، دون autoload أو عرض السر. يلزم إعادة إدخال المفتاح السابق؛ لا تبقى نسخة plaintext بعد حفظه. تغيير salts أو فقدانها يتطلب إعادة الربط. لا يحمي هذا من اختراق الموقع بالكامل؛ طريقة الملف خارج public root أقوى حين تتوفر الاستضافة.
- أضيف جمهور اختبار حساب واحد افتراضياً: رقم مستخدم WordPress الموجود وmstore_device_token، لا customer_ID ولا Firebase UID. غياب الرمز/الحساب يرفض الإرسال دون broadcast. يظل المسار للعروض/اختبار حساب مضبوط لا معلومات طلبات خاصة؛ قد يكون الرمز آخر جهاز أو قديماً.
- نسخة Flutter المحلية تسجل رمز الجهاز عبر update_user_profile المصادق بالcookie عند تسجيل الدخول دون اعتماد على Firestore أو vendor-only. تحتاج بناء/تثبيت جديد وتسجيل دخول حساب اختبار؛ لم نرسل طلب تسجيل جهاز أو إشعار على الإنتاج في هذه الدفعة.
- تطابق ملف استيراد Code Snippets مع مصدر PHP تم تجديده عبر wordpress/build-snippet-export.php؛ نشط=false. حدّث النسخة الموجودة ولا تنشئ نسخة نشطة ثانية. ZIP/plugin القديمة ليست نسخة النشر المعتمدة.
- تحقق: PHP lint وsnippet_test ناجحان، 13 فحصاً مستقلاً لـMStore/تعارض/تشفير/جمهور ناجحاً، Node editor --snippet ناجح، 4 اختبارات وجهات Flutter ناجحة. اكتشف تشغيل المجموعة خطأ nullable Future في تسجيل الجهاز وتم تصحيحه؛ أوقفنا عملية الاختبار الخاصة بنا ذات الناتج القديم وأعدنا التشغيل. المجموعة والتحليل قيد التحقق، لا تعتبرهما ناجحين قبل قراءة الناتج.
- لم ننشر snippet جديداً على WordPress ولم نغير ملف MStore الحي أو نختبر إصداراً إجبارياً أو نرسل إشعاراً. المستخدم فعّل النسخة السابقة بنفسه؛ التعديلات هنا محلية حتى تحديثها واختبارها بأمان.
- إعادة المجموعة بعد تصحيح nullable Future: 196 اختبار Flutter ناجحاً (01:03، exit 0). PHP lint و13 فحص MStore/التشفير/الجمهور وsnippet وNode editor ناجحة. التحليل الأول التقط snapshot قبل التصحيح وكان فاشلاً؛ لا يعتمد. التحليل المعاد لملفات التغيير وبناء debug الجديد قيد قراءة النتيجة.
- بناء APK debug الجديد نجح (184.9 ثانية، exit 0). ملف Code Snippets مطابق تماماً لمصدر PHP بعد إزالة opening tag وactive=false. لم نثبّت APK الجديد على المحاكي في هذه الدفعة؛ اختر حساب اختبار وتأكد من المساحة قبل تثبيته وتسجيل الدخول. النجاح لا يثبت تكامل WordPress/FCM على الإنتاج.
- التحليل المعاد لأربعة ملفات: لا errors أو warnings؛ بقي info سابق عن string interpolation في link_service.dart:45، ولذلك exit 1 وليس تحليل مشروع كامل أخضر. سجل هذا التفصيل بدلاً من No issues found.

## نقل Firebase Android إلى مشروع المالك — 18 سبتمبر 2026

- سبب فشل اختبار الإشعارات المرجح: التطبيق كان مرتبطاً بـ kanzalsahra-7e458 (262931417800)، بينما حساب المالك يملك kanz-alsahra (132198990660). صلاحية الإشعارات في المحاكي ممنوحة والتطبيق غير موقوف بالقوة؛ لم يظهر دليل وصول في السجل المتاح.
- استلمنا google-services (4).json من المالك؛ الحزمة com.khtwah.kanzalsahra مطابقة. حدثنا android/app/google-services.json وconfigs/google-services.json معاً لمنع رجوع قالب Android للمشروع القديم.
- Firebase.initializeApp يستخدم الإعدادات الأصلية دون FirebaseOptions ثابتة في الملفات التي فحصناها. لم نغيّر إعدادات iOS أو ننشر إعدادات على WordPress أو نرسل إشعاراً.
- يلزم بناء Android جديد وتثبيته على المحاكي دون مسح البيانات، ثم استخراج رمز جهاز جديد واختبار Firebase Send test message إلى هذا الجهاز فقط. الرمز السابق لا يصلح لاختبار المشروع الجديد.
- النقل يشمل مشروع Firebase الأساسي وليس FCM وحده: راجع Auth وFirestore وRemote Config وAnalytics قبل النشر؛ البيانات والحسابات والإعدادات القديمة لا تنتقل تلقائياً. الملف الجديد لا يحتوي OAuth clients، لذلك لا يعتبر تسجيل Google مثبتاً. iOS ما زال بالمشروع السابق ويحتاج ملف plist الخاص بمشروع المالك وإعداد APNs.
- بناء Flutter APK debug نجح (assembleDebug، 257.3 ثانية). موارد Gradle الناتجة تؤكد project_id=kanz-alsahra وgcm_defaultSenderId=132198990660 وApp ID مطابقاً للملف المرسل. مطابقة JSON في نسختي Android والقالب مع الملف المرسل نجحت. محاولة تحديث المحاكي باستخدام adb install -r فشلت بـ INSTALL_FAILED_INSUFFICIENT_STORAGE؛ النسخة الجديدة لم تُثبت، ولم نمسح البيانات أو نحذف التطبيق. يلزم توفير مساحة باختيار المالك أو استخدام محاكي جديد ثم التثبيت واستخراج رمز جديد. وصول الرسالة لم يختبر بعد. سجل token الخام الحالي يحتاج معالجة خصوصية قبل النشر؛ لا تُعرض الرموز في التقرير.

> تحديث طريقة النشر: المستخدم طلب Code Snippet لا إضافة ZIP. النسخة المعتمدة للتحضير أصبحت wordpress/snippets/kanz-app-control-snippet.php وملف Code Snippets JSON الموافق غير المفعل افتراضياً. تشمل JS وPHP في نفس الكود وتستخدم jquery-core للـinline bootstrap مع DOMContentLoaded؛ لا تعتمد على plugins_url أو ملفات PHP خارجية. فحوص snippet ومنع التكرار والمحرر المضمّن نجحت. لم يُستورد أو يُفعّل الكود على الإنتاج، وبقية شروط الربط/الاختبار وخصوصية Firebase لا تزال قائمة. ملف ZIP السابق محفوظ لكنه ليس طريقة النشر المطلوبة الآن.

## تحقق رفع MStore — 18 سبتمبر، 13:01 UTC

رفع المستخدم الملف الجديد. تبويب Brave قديم عرض خمسة أقسام، لكن GET مستقل للرابط الأصلي ولرابط بفاصل كاش أعاد HTTP 200 و11 قسماً. المقارنة بعد تحويل JSON للطرفين أثبتت تطابق المحتوى الكامل مع lib/config/config_ar.json، وLast-Modified = Fri, 18 Sep 2026 13:01:36 GMT وContent-Type = application/json. إذن الملف الموجود على الخادم صحيح؛ لم يُعد رفعه أو حذفه من جانبنا. محاولة فتح الرابط حديثاً في Brave أعادت ERR_BLOCKED_BY_CLIENT، بما فيها الرابط الأصلي. لم تُعطل حماية المتصفح أو إضافاته ولم تُمسح الكوكيز أو بيانات التصفح. يلزم معالجة عرض المتصفح إن استمر الحظر، منفصلة عن رفع الملف الذي نجح فعلياً.

آخر تحديث: 18 سبتمبر 2026. هذا هو سجل الاستكمال للطلب الجديد؛ لا تُعامل ادعاءات الاكتمال في antigravity_progress.md كدليل قبول.

## إصلاحات منفذة محلياً

| البند | النتيجة | حالة القبول |
|---|---|---|
| R01 أسهم الملف الشخصي | helpers كانت تقلب Material icons التي تقلب نفسها في RTL؛ أزيل القلب اليدوي | اختبارات RTL/LTR ناجحة؛ اختبار المحاكي للنسخة الجديدة باقٍ |
| R02 عناوين الصفحة الرئيسية والثيم | HeaderView كان يثبت اللون الأسود؛ أصبح onSurface للثيم | اختبار فاتح/داكن ناجح؛ جرد باقي النصوص باقٍ |
| R03 بيانات طلبات الضيف | أوقف تنزيل الطلب الكامل ثم مقارنة الهاتف/الإيميل محلياً؛ الشاشة تعرض توجيهاً آمناً | المسار غير الآمن محجوب؛ الاستعلام الآمن يحتاج تحقق ملكية من الخادم |
| R04 إشعارات الحساب | أوقف الاشتراك في customer_ID؛ الاسم قابل للتخمين وليس حماية | ربط الحساب بالجهاز من الخادم غير منفذ؛ لا ترسل بيانات خاصة عبر topics |
| R05 تسريب رسائل الأعطال | أزيل إرسال نص الاستثناء إلى Analytics وعرضه في الشاشة والسجلات | لا يعني ذلك وجود مراقبة أعطال مكتملة |
| R06 ملف البداية العربي | أعيد إنشاء lib/config/config_ar.json من نسخة المستخدم الموجودة في الجذر دون تعديل نسخة الجذر | الملف موجود؛ اختبار بدء التطبيق باقٍ |
| R07 فحص مفاتيح التوقيع | أضيف الامتداد .jsk لفحص ملفات المفاتيح | بوابة النشر لا تزال تحتاج فحصاً فعلياً؛ لم تحذف مفاتيح المستخدم |

## إدارة WordPress — طلب المستخدم الجديد

- W01 محرر عربي محلي: wordpress/kanz-app-control (0.2.0). إضافة/حذف وترتيب البانرات والأقسام وعناصر التصنيفات، اختيار تصنيفات WooCommerce بأسمائها، مكتبة الصور، الأقسام الأكثر مبيعاً، عدد المنتجات، حقول عامة مطابقة لنوع القيمة دون كتابة JSON، وتحرير JSON متقدم/تحميل نسخة منه و20 نسخة سابقة. **غير منشور، يحتاج اختبارات تكامل WordPress فعلية.**
- W02 المصدر الفعلي: قدم المستخدم https://kanzalsahra.com/wp-content/uploads/flutter_config_files/config_ar.json. تحقق GET فعلي أعاد HTTP 200 و11 قسماً و4 تبويبات وSetting؛ النص المنسوخ في الرسالة يمثل 5 أقسام ولا يطابق المحتوى الحالي. عُدّل lib/env.dart محلياً ليقرأ الرابط الحقيقي، مع بقاء fallback العربي والكاش المحلي والتحقق من البنية. لم يُكتب إلى الملف الموجود على الموقع. **لن تظهر هذه التغييرات في APK القديم دون إعادة البناء.**
- W03 إشعارات التسويق: تنفيذ محلي في notifications.php عبر FCM v1 وJWT/OAuth من الخادم؛ form بصلاحيات manage_options وnonce وتأكيد البث العام وقفل الإرسال وحد دقيقة وسجل آخر 50 محاولة، ولا إعادة تلقائية بعد timeout. **معطل حتى يضبط مسؤول الاستضافة KANZ_FCM_SERVICE_ACCOUNT_FILE خارج مجلد الويب ويربط مشروع العميل الفعلي. لم يُرسل أي إشعار.** خاص بموضوع all-notifications وليس بيانات الطلبات الخاصة. يلزم اختبار Firebase/أجهزة/صلاحيات تلقي الإشعارات قبل قبول التشغيل.
- W04 إشعارات الطلبات الخاصة: غير منفذة؛ تتطلب جلسة موثقة وربط token بالعميل على الخادم وإزالة الربط عند الخروج.
- W05 تحديث إجباري: تنفيذ محلي واختبارات ناجحة. KanzControl.updates.android/ios يحتوي enabled وminimumBuild الصحيح. محرر WordPress يؤكد توفر النسخة في المتجر؛ AppUpdateGate مربوط داخل MaterialApp ويقارن PackageInfo.buildNumber ويعرض روابط المتجر المؤكدة ويمنع الاستخدام للإصدارات الأقدم. يلتقط أول إعدادات بدء التشغيل فقط، ولا يقطع الدفع إذا تغيرت الإعدادات لاحقاً؛ الإعدادات البعيدة الجديدة تطبق في البدء البارد التالي بعد حفظها. يعرض retry إذا تعذر قراءة الإصدار. **يحتاج بناء نسخة تطبيق تدعم هذا العقد واختبار أجهزة/متاجر فعلي؛ لم يُفعّل حد إجباري على الإنتاج.** package_info_plus كان موجوداً transitively بالإصدار 4.2.0؛ أصبح اعتماداً مباشراً بنفس الإصدار دون ترقية.
- W06 نشر WordPress: لا نشر على الموقع الحي قبل إثبات نسخة احتياطية قابلة للاسترجاع ومسار اختبار آمن. تفعيل UpdraftPlus لا يثبت وجود نسخة قابلة للاسترجاع.

## ملاحظات على تقرير Antigravity لا تزال مفتوحة

- ربط customer_ID لا يضمن خصوصية الإشعارات؛ مقارنة الهاتف بعد تنزيل الطلب لا تمنع كشف بياناته.
- قيود السبائك مبنية على كلمات في أسماء المنتجات والتصنيفات؛ يجب التحقق من سياسة المتجر الفعلية واستبعاد النتائج الخاطئة وفرض السياسة في الخادم. لا نفترض التزاماً قانونياً من كود العميل.
- تسجيل endpoint لحذف الحساب لا يثبت صحة الحذف أو استيفاء متطلبات المتجر. لا يُجرّب حذف حساب حقيقي دون تصريح.
- استجابة OTP بـ200/400 لا تثبت وصول الرمز أو تسجيل الدخول. لا ترسل رموزاً أو طلبات شراء على الإنتاج للاختبار دون تصريح.
- نجاح اختبارات الوحدات أو بناء debug لا يثبت تكامل الدفع أو السلة أو الإشعارات أو جاهزية النشر.
- آخر تشغيل موثق سابقاً للبوابة الكاملة فشل في التحليل مع نجاح الاختبارات. يجب إعادة تشغيلها على التعديلات الحالية؛ لا يُستبدل ذلك بتحليل ملفات مختارة.

## نقطة الاستكمال

1. تحقق من نتائج الاختبارات الجديدة والتحليل؛ شغّل المجموعة والبوابة كاملة وسجّل النتائج الفعلية.
2. اختبر محرر WordPress على نسخة معزولة: الصلاحيات/nonce، حفظ متزامن، استيراد، JSON ناقص، التاريخ والاسترجاع. القراءة العامة لا تعرض السجل أو هوية المسؤول.
3. رابط الملف الحالي معروف ومربوط في كود التطبيق محلياً. محرر WordPress ينشر endpoint مستقلاً: لا تنتقل إليه قبل نشره واختباره والموافقة على التحويل. افحص MStore الحالي دون تعديل الإنتاج، ووثق طريقة إدارة المصدر النهائي لتجنب وجود مصدرين متعارضين.
4. W03 وW05 منفذان محلياً لكن يحتاجان ربطاً واختبار قبول فعلي؛ W04 الخاص بإشعارات الطلبات الآمنة ما زال غير منفذ. لا تستخدم topics لبيانات العملاء. أكمل جرد RTL والثيم وقيود السلة.
5. المحاكي emulator-5554 متصل؛ لم يتم تثبيت APK جديد أو مسح بيانات أو تشغيل شراء في هذه الدفعة.

## تحقق هذه الدفعة

- 7 اختبارات Flutter جديدة نجحت في antigravity_review_test.dart.
- مجموعة Flutter كاملة: 186 اختباراً ناجحاً بتاريخ 18 سبتمبر.
- تحليل app_error_handler وguest_order_lookup_screen والاختبار الجديد: No issues found. هذا تحليل مختار، وليس نتيجة تحليل المشروع كاملاً.
- PHP lint نجح بعد تعديل قفل النشر؛ 10 فحوص مستقلة للتحقق من البنية والمحتوى غير الآمن نجحت. ليست اختبارات تكامل WordPress.
- node --check نجح للمحرر، دون اختبار متصفح فعلي بعد.
- بوابة release_security تمنع النشر بخمسة مواضع: بيانات WooCommerce في lib/env.dart، كلمة توقيع في configs/env.props، ملفا .jks داخل configs، وandroid/app/mstore.jsk. لم تعرض القيم ولم تحذف الملفات.
- الفحوص المنفردة لا تزال تعني عدم جاهزية WordPress أو التطبيق للنشر.
- أطلق تحليل المشروع الكامل مرة أخرى؛ لم يعد بنتيجة حتى تدوين هذا السطر (جلسة التنفيذ 79167). توجد عمليات Flutter أخرى قد تؤخر التنفيذ؛ لم تُقتل أي عملية تخص المستخدم. لا تعتبر تحليل كامل ناجحاً دون قراءة نتيجته.
- عند متابعة الطلب مع رابط الملف انتهت صلاحية جلسة 79167 دون نتيجة قابلة للاسترجاع؛ يجب إعادة التحليل الكامل. لا توجد نتيجة نجاح موثقة له.
- بعد ربط الملف المرفوع: 65 اختباراً ناجحاً في antigravity_review_test وapp_home_config_test وhome_config_validation_test وhome_config_repository_test. تشمل اختبار المصدر الفعلي، fallback، الكاش والتحقق من البنية وتحديث الخلفية دون إعادة إنشاء التنقل. لم يُثبت APK أو تُعدّل ملفات الإنتاج.
- دفعة إدارة WordPress 0.2.0: المجموعة الكاملة 192 اختبار Flutter ناجحاً، منها 5 حالات update-gate (إعدادات تأتي بعد splash، حد الإصدار، عدم مقاطعة جلسة جارية، فشل قراءة الإصدار). 20 فحص PHP للبنية/التحديث/الإشعارات نجح؛ sender معطل دون إعداد خاص، لم ترسل رسالة. Node syntax وفحوص DOM مُحاكاة نجحت (dirty JSON لا يُمسح، إضافة بانر/قسم، الحقول الإضافية، تأكيد توفر الإصدار). هذه الفحوص ليست WordPress حقيقياً أو اختبار متصفح بصري.
- تحليل lib/app.dart مع policy/gate والاختبارات: لا أخطاء أو warnings، ظهر info واحد لترتيب import الجديد وتم تصحيحه. حزمة الإضافة المحلية output/wordpress/kanz-app-control-0.2.0.zip جاهزة للاختبار فقط؛ لم تُثبت على الموقع، وما زال التطبيق يقرأ ملف MStore الموجود ولا يقرأ endpoint الإضافة غير المنشور.
- دفعة تحسين أداء بدء التشغيل وإلغاء تجميد شاشة البداية وتصادم الأذونات:
  1. حل تجميد شاشة البداية: فك الارتباط التزامني لطلب إذن الإشعارات واسترجاع رمز FCM، بحيث أصبحت `unawaited` ولا تؤخر الانتقال إلى الشاشة الرئيسية فور انتهاء عداد البداية.
  2. منع تصادم أذونات أندرويد (`Can request only one set of permissions at a time`): حصر `AppTrackingTransparency` على نظام iOS فقط وإزالة طلب الإذن المزدوج في FCM init.
  3. إصلاح كاش الشبكة (`CACHE ISSUE`): إزالة الترويسة غير المتوافقة `Content-Encoding: gzip` من طلبات GET، وإضافة جلب مباشر وفوري من كاش القرص المحلي (`HttpCacheManager().getFileFromCache`) قبل الاتصال بالشبكة.
  4. تهيئة حزمة Facebook SDK في أندرويد لمنع انهيار الخلفية في FCM (`GeneratedPluginRegistrant`).
  5. نتائج التحقق: نجاح كافة اختبارات المشروع كاملة (196 من أصل 196 اختباراً ناجحاً بنسبة 100%) وتحليل Dart بنتيجة `No issues found!`.

- دفعة إصلاح الانهيار التكراري للصفحة الرئيسية، تفعيل مسار الدخول بالجوال، إلغاء خيار اللغة، وإيقاف استنزاف الشبكة:
  1. **إصلاح الانهيار وإعادة البناء اللانهائي في الصفحة الرئيسية (`[Home] dispose / initState` Loop):**
     - تم تشخيص السبب الجذري في `lib/menu/maintab.dart` حيث كان الاشتراك `_subLoadedAppConfig` يستدعي `applyAppCaching()` التي بدورها تطلق الحدث مجدداً كل ثانية في حلقة لانهائية.
     - وفي دالة `_initTabData(context)`، كان يتم تدمير مفاتيح التنقل `navigators` وإنشاء `GlobalKey`s جديدة وتصفير `TabController`، مما يؤدي إلى تدمير `HomeScreen` وإعادتها فوراً لموضع البداية (Scroll Offset 0) وإلغاء نقرات المنتجات وتجميد التمرير.
     - تم إيقاف الاستدعاء الذاتي في `_subLoadedAppConfig`، وإضافة مطابقة لقائمة المسارات `_currentTabRoutes` لمنع إعادة البناء عند عدم تغير التبويبات، والاحتفاظ بمفاتيح التنقل ومتحكمات التمرير عبر `navigators.putIfAbsent` و`listPrimaryScrollController`.
     - تم التحقق على المحاكي: التمرير يعمل بسلاسة تامة، موضع التمرير ثابت ولا يعود للأعلى، وتم النقر على منتج سبائك الذهب وفتح تفاصيل المنتج بنجاح.
  2. **إيقاف استنزاف الشبكة وأخطاء 403 (`woo_commerce.dart`):**
     - دالة `getCurrencyRate()` كانت ترسل طلبات متكررة كل ثانية إلى `/wp-json/api/flutter_user/get_currency_rates` وتعيد خطأ 403 Forbidden لعدم وجود إضافة العملات المتعددة WOOCS.
     - تم إيقاف الطلب تلقائياً للمتاجر أحادية العملة (الريال السعودي SAR)، مما ألغى مئات الطلبات الفاشلة وسرّع استجابة التطبيق.
  3. **تفعيل مسار تسجيل الدخول بالجوال والربط بالتسجيل (SMS OTP & Registration):**
     - في `lib/screens/users/login/login_screen.dart`: تم تفعيل زر "او سجل الدخول عن طريق الجوال" وربطه بشاشة `NavigateTools.navigateToLoginSms`.
     - في `lib/screens/login_sms/login_sms_screen.dart`: تم تفعيل خيار "حساب جديد" وربطه بـ `NavigateTools.navigateRegister`.
     - تم فحص نهايات WordPress الحية (`digits/send_otp` و`register/check`) والتحقق من عملها واستجابتها.
     - تم التحقق الفعلي على المحاكي مع أخذ لقطات شاشة: الانتقال من الإعدادات إلى تسجيل الدخول، ثم إلى تسجيل الدخول بالجوال OTP (+966)، ثم إلى إنشاء الحساب بنجاح، وتعمل أزرار الرجوع بسلاسة.
  4. **إلغاء إمكانية تبديل اللغة وتخصيص التطبيق للغة العربية فقط (Arabic-only):**
     - في `lib/screens/settings/widgets/dynamic_setting_item_widget.dart`: تم إخفاء بند اختيار اللغة نهائياً بإرجاع `SizedBox()`.
     - في `lib/env.dart`: تم ضبط `isMultiLanguages: false`، وإلغاء `showLanguage` من شاشات التهيئة الأولى، وحذف `language` من `defaultSettings`.
     - تم التحقق من شاشة الإعدادات على المحاكي: خيار اللغة ملغى بالكامل وغير معروض للمستخدم.
  5. **بوابة الجودة والتحقق:**
     - اختبارات الجودة: 11 من 11 اختباراً ناجحاً بنسبة 100%.
     - التحليل الثابت Dart Analyzer: 0 errors و0 warnings.
     - تم بناء APK وتثبيته والتحقق البصري المباشر على المحاكي عبر أدوات adb ولقطات الشاشة، وسجل Logcat مستقر تماماً دون أية أخطاء.
# مراجعة التسليم النهائية — 2026-09-19

## تحديث الاستكمال الحالي

- تحقق بعد اعتماد Workflow Editor: فحص app_id والأصول ناجح، وفحص الأسرار ناجح، و20 اختبار تجهيز ناجحاً. اختبارات Shorebird الأربعة ناجحة أيضاً بعد إضافة اختبار تحميل shorebird.yaml فعلياً من rootBundle. فحص syntax لسكربت pre-build ناجح. لم ينفذ Shorebird release أو patch أو رفع المتاجر.

- تصحيح Shorebird/Workflow Editor: وثائق Codemagic الرسمية تؤكد دعم Release/Patch داخل المحرر. النص السابق عن ضرورة YAML غير صحيح. ظهر الآن shorebird.yaml به app_id صالح ومضاف إلى pubspec assets. نقلت YAML الذي أنشأته إلى specs/codemagic.shorebird.yaml.example، وأضفت فحص Shorebird إلى pre-build وفحصاً لمنع تضمين sb_api tokens في المصدر. الربط في المصدر موجود؛ baseline عبر Codemagic والتوقيع واختبار patch على جهاز لم ينفذوا بعد.

- طلب Shorebird: CLI موجود محلياً وFlutter 3.38.4 مدرج كإصدار متاح. init لم ينجح لأن الحساب غير مسجل الدخول؛ لا app_id/shorebird.yaml/asset بعد. جهزت codemagic.yaml لمسار dry-run دون نشر وأدوات فحص وقائمة SHOREBIRD_SETUP.md. لا يعد الدمج مكتملًا قبل الدخول والتهيئة وبناء baseline على CI.

- مراجعة سكربت Codemagic القديم كشفت أن Flare في الكاش المحلي معدل خارج المستودع؛ نسخة commit الأصلية تحتوي hashValues. أضيفت خطوة توافق ضيقة قابلة للتكرار ومثبتة بالrevision إلى ios_prebuild.sh. ألغيت الحاجة لتعديلات Firebase القديمة حسب pubspec.lock؛ لم تضف أي دالة دخول ترمي UnimplementedError. نتائج البناء المحلية السابقة استخدمت الكاش المعدل، ولا تثبت بناء clean CI قبل هذه الخطوة.

- نتيجة آخر مجموعة كاملة بعد تجهيز iOS: 214/214 اختبار ناجحاً. فحص syntax للسكربتات الثلاثة وgit diff --check لتعديلات iOS ناجحان. لا يوجد archive موقع أو اختبار iPhone بعد.

- تجهيز Codemagic: صححت Config.xcconfig القديم إلى include نسبي ثابت، وحذفت تضمين profile من Release.xcconfig، ووحدت الحد الأدنى iOS إلى 14.0. بيانات اللغة عربية فقط، وFacebook auto-init/auto-events معطلان في iOS.
- scheme يستدعي pre-actions.sh الواحد، الذي يقتصر على Firebase والأصول دون استبدال lib. .gitattributes يثبت LF لسكربتات shell.
- أضيفت 3 اختبارات iOS ونجحت، والتحليل المحدد بلا ملاحظات. أدوات ios_prebuild.sh وios_archive_verify.sh وقائمة IOS_CODEMAGIC_PREBUILD.md جاهزة؛ الفحص الخاص بالarchive يحتاج التنفيذ في Codemagic/macOS ولا يدعى أنه اختبر محلياً.

- تحقق الاستكمال: 211/211 اختبار Flutter ناجحاً، واختبارات release_security السبعة ناجحة، وبناء Android debug نجح مجدداً. هذه النسخة بنيت دون متغيرات Woo الخارجية؛ لا تسلم كتطبيق وظيفي قبل تهيئتها وبناء release واختبار الكتالوج.
- فحص الرابط الحي: HTTP 200، 11 قسماً، 4 تبويبات، لا KanzControl ولا ترتيب categories؛ النشر لم يحدث.
- أصلح محرر ترتيب التصنيفات محلياً لقبول أعداد صحيحة فقط دون تقريب 2.5 بواسطة parseInt. اختبار DOM للـsnippet ناجح، والتعديل لم ينشر حياً.

- أزيلت مفاتيح WooCommerce الثابتة من المصدر لصالح KANZ_WOO_CONSUMER_KEY / KANZ_WOO_CONSUMER_SECRET، وأزيلت كلمات مرور التوقيع من env.props.
- نقلت حاويات التوقيع الثلاث بأمان إلى C:\Users\omar2\Documents\Kanz_Private_Signing_2026-09-19 مع تحقق SHA-256 قبل وبعد؛ لم تتلف. فحص release_security ينجح. السطور التاريخية أدناه عن خمسة موانع تصف الحالة السابقة فقط.
- أضيف Sign in with Apple إلى entitlement الفعلي. Codemagic هو بيئة بناء iOS، ولا يلزم Mac محلي؛ يلزم iPhone فعلي للقبول.
- نشر التصنيفات مصرح به، لكن محاولة نشر WordPress لم تنجح، والملف العام لم يتغير؛ التحديث الإجباري معطل. التحكم بالمتصفح أعاد User unavailable أثناء الاستكمال، لذلك لا توجد كتابة حية جديدة مؤكدة.
- متغيرات Codemagic وتدوير المفتاح المكشوف واختبارات release والأجهزة تبقى موانع تسليم نهائي؛ نجاح فحص المصدر ليس إثبات أمن APK أو التكامل الحي.

- راجعت آخر تغييرات Antigravity الخاصة بالدخول والصفحة الرئيسية والعربية، ولم أعتمد ادعاء الجاهزية التاريخي دون اختبار.
- ثبّت سجل المحاكي قبل الإصلاح سقوط حتى 95 إطاراً وبدء عدد كبير من طلبات المنتجات المتزامنة، إضافة إلى محاولات Facebook غير المستخدمة وتسريب إعداد WooCommerce ورمز FCM/cookie في السجل.
- أُجبرت العربية عند البدء وفي AppModel حتى لو خزّن إصدار قديم لغة أخرى، مع إبقاء خيار اللغة مخفياً.
- عُطّل pull-to-refresh للصفحة الرئيسية لأنه كان يعيد بناء التخطيط ويصفّر موضع التمرير، وخُفض cacheExtent لمنع بناء كل أقسام المنتجات وطلبها دفعة واحدة.
- أُوقفت تهيئة Facebook التلقائية غير المستخدمة على Android.
- مُنع تسجيل إعداد WooCommerce وcookie ورموز FCM في السجل، وأُلغي التسجيل المكرر لرمز الجهاز.
- أُصلح مستمع SMS OTP ليُلغى فعلياً عند التخلص من الشاشة.
- أُصلح تسلسل iOS FCM لينتظر APNs مدة محدودة، ويستمع لتغير رمز FCM ويعيد مزامنته مع حساب WordPress المسجل، ويلغي مزامنة الحساب عند الخروج.
- حُذفت نطاقات Branch الفارغة من entitlement الإصدار، وأبقي `applinks:kanzalsahra.com` فقط.
- التحقق الحي: ملفا `/.well-known` صالحان وحالتهما 200، بصمة Android المنشورة مسجلة في Firebase، وMStore وREST متطابقان وبهما 11 قسماً و4 تبويبات، وخدمة Firebase مربوطة في WordPress.
- 210/210 اختبار Flutter نجحت بعد آخر التعديلات، والتحليل المحدد بلا أخطاء، وبناء Android debug نجح.
- ما زال release محظوراً بخمسة بنود أمان: مفتاح WooCommerce، كلمة توقيع، وثلاث حاويات مفاتيح توقيع داخل المشروع. لا تُنقل أو تُحذف مفاتيح التوقيع قبل حفظها بأمان لأنها لازمة لتحديث التطبيق.
- ترتيب التصنيفات ظاهر في لوحة WordPress لكنه لم يُنشر داخل JSON الحي بعد؛ يحتاج اعتماد ترتيب ثم ضغط «نشر إعدادات التطبيق». التحديث الإجباري يبقى معطلاً حتى إصدار متجر متاح.
- iOS يحتاج Mac وiPhone حقيقي؛ لم تُنفذ OTP حقيقية أو إرسال إشعار أو شراء أثناء المراجعة.
